A : Creating Words
Code :

#include <iostream>
#include <string>
using namespace std;
 
int main() {
    int t;
    cin >> t;
 
    // Process each test case
    for (int i = 0; i < t; ++i) {
        string a, b;
        cin >> a >> b;
        
        // Swap the first characters
        char temp = a[0];
        a[0] = b[0];
        b[0] = temp;
 
        // Output the result
        cout << a << " " << b << endl;
    }
 
    return 0;
}


